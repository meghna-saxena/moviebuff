import * as React from 'react'
import * as config from '../../../config';
import './Actor.css';

const Actor = (props) => {
    return (
        <div>
            Actor
        </div>
    )
}

export default Actor;
